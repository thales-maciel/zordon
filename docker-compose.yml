services:
  lb:
    build: .
    image: ghcr.io/thalesmaciel/zordon:local
    container_name: zordon-lb
    entrypoint: ["/app/zordon-lb"]
    environment:
      LB_PORT: "9999"
      LB_BACKENDS: "/sock/api1.sock,/sock/api2.sock"
    ports:
      - "9999:9999"
    volumes:
      - sockets:/sock
    depends_on:
      - api1
      - api2
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: "24MB"

  api1:
    build: .
    image: ghcr.io/thalesmaciel/zordon:local
    container_name: zordon-api1
    environment:
      ZORDON_MODEL_PATH: "/app/model/references.i16.bin"
      ZORDON_UNIX_PATH: "/sock/api1.sock"
    volumes:
      - sockets:/sock
    deploy:
      resources:
        limits:
          cpus: "0.45"
          memory: "163MB"

  api2:
    build: .
    image: ghcr.io/thalesmaciel/zordon:local
    container_name: zordon-api2
    environment:
      ZORDON_MODEL_PATH: "/app/model/references.i16.bin"
      ZORDON_UNIX_PATH: "/sock/api2.sock"
    volumes:
      - sockets:/sock
    deploy:
      resources:
        limits:
          cpus: "0.45"
          memory: "163MB"

volumes:
  sockets:
